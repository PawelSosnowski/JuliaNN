# JuliaNN
## Neural network library implemented in Julia 

File `graph.jl` contains operational graph definition (for autograd) and base operations.

In `examples` directory are jupyter notebook examples of how library can be used at current state.

TODO:

- modules for optimizers, layers and predefined trainers
- cross-entropy loss
- convolutional layers
